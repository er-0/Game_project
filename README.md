Flying game! The file pilot_game_database.txt includes the instructions to edit the already existing flight_game database.

Create a .env in the root folder, don't forget to include your MariaDB username and password.

 

```.env
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
```
